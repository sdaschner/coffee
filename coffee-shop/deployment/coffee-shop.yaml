kind: Service
apiVersion: v1
metadata:
  name: coffee-shop
  labels:
    app: coffee-shop
spec:
  selector:
    app: coffee-shop
  ports:
    - port: 9080
      name: http
---

kind: Deployment
apiVersion: apps/v1beta1
metadata:
  name: coffee-shop
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: coffee-shop
        version: v1
    spec:
      containers:
      - name: coffee-shop
        image: sdaschner/coffee-shop:12factor-1
        imagePullPolicy: Always
        ports:
        - containerPort: 9080
        livenessProbe:
          httpGet:
            path: /
            port: 9080
          initialDelaySeconds: 40
        readinessProbe:
          httpGet:
            path: /coffee-shop/resources/health
            port: 9080
          initialDelaySeconds: 60
        env:
        - name: DEVELOPMENT
          valueFrom:
            configMapKeyRef:
              name: coffee-shop-config
              key: developmentMode
      restartPolicy: Always
---

kind: ConfigMap
apiVersion: v1
metadata:
  name: coffee-shop-config
data:
  developmentMode: "true"
---
